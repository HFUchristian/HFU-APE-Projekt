<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:MyFirstApp.ViewModels"
             x:DataType="viewmodels:ItemDetailViewModel"
             x:Class="MyFirstApp.Views.ItemDetailPage"
             Title="{Binding Title}">

    <StackLayout Spacing="20" Padding="15">
        <Label Text="Text:" FontSize="Medium" />
        <Label Text="{Binding Text}" FontSize="Small"/>
        <Label Text="Description:" FontSize="Medium" />
        <Label Text="{Binding Description}" FontSize="Small"/>
        <Label Text="Description 2:" FontSize="Medium" />
        <Label Text="{Binding Description2}" FontSize="Small"/>
        <Label Text="Age:" FontSize="Medium" />
        <Label Text="{Binding Age}" FontSize="Small"/>
    </StackLayout>
    
</ContentPage>